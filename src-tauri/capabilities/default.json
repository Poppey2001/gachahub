{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the GachaHub main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
